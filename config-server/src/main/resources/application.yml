server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sherzodn/yoke-project.git
          force-pull: true
          search-paths: configuration


