spring:
  application:
    name: yoke-event
  cloud:
    config:
      uri: http://localhost:8888
      profile: dev

server:
  port: 8083

yoke:
  notify:
    url: http://YOKE-NOTIFY/event
    #url: http://localhost:8082/event